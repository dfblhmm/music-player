import { PureComponent, Fragment } from 'react'
import { getPath } from 'redux/actions/navLink'
export default class FooterContainer extends PureComponent {
  render() {
    return (
      <Fragment>
        底部
      </Fragment>
    )
  }
}

